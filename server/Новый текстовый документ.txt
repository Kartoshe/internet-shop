-- Пользователи
CREATE TABLE IF NOT EXISTS users (
  id SERIAL PRIMARY KEY,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  name VARCHAR(100),
  created_at TIMESTAMP DEFAULT NOW()
);

-- Товары (теперь с 7 полями)
CREATE TABLE IF NOT EXISTS products (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  description TEXT NOT NULL,
  category VARCHAR(100) NOT NULL,
  image_url VARCHAR(255) NOT NULL,
  rating DECIMAL(3, 2) DEFAULT 0.0,
  stock_quantity INTEGER DEFAULT 0,
  specifications JSONB -- Дополнительные характеристики в формате JSON
);

-- Заказы
CREATE TABLE IF NOT EXISTS orders (
  id SERIAL PRIMARY KEY,
  user_id INTEGER REFERENCES users(id),
  total_price DECIMAL(10, 2) NOT NULL,
  status VARCHAR(50) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW()
);

-- Товары в заказе
CREATE TABLE IF NOT EXISTS order_items (
  order_id INTEGER REFERENCES orders(id),
  product_id INTEGER REFERENCES products(id),
  quantity INTEGER NOT NULL,
  PRIMARY KEY (order_id, product_id)
);

-- Тестовые данные (7 товаров)
INSERT INTO products (name, price, description, category, image_url, rating, stock_quantity, specifications) VALUES
('Ноутбук ASUS ROG Strix', 89990, 'Игровой ноутбук с экраном 15.6", процессор Intel Core i7', 'Электроника', 'https://example.com/laptop.jpg', 4.7, 10, '{"processor": "Intel Core i7", "ram": "16GB", "storage": "512GB SSD"}'),
('Смартфон iPhone 14', 79990, 'Флагманский смартфон с камерой 48 Мп', 'Электроника', 'https://example.com/iphone.jpg', 4.9, 15, '{"screen": "6.1", "battery": "3279 mAh"}'),
('Наушники Sony WH-1000XM4', 24990, 'Беспроводные наушники с шумоподавлением', 'Электроника', 'https://example.com/headphones.jpg', 4.8, 8, '{"type": "over-ear", "battery_life": "30 hours"}'),
('Книга "Чистый код"', 1200, 'Руководство по написанию читаемого и поддерживаемого кода', 'Книги', 'https://example.com/book.jpg', 4.9, 20, '{"author": "Роберт Мартин", "pages": 464}'),
('Кофемашина DeLonghi', 34990, 'Автоматическая кофемашина для дома', 'Бытовая техника', 'https://example.com/coffee.jpg', 4.6, 5, '{"power": "1450W", "water_tank": "1.8L"}'),
('Фитнес-браслет Xiaomi', 2990, 'Умный браслет с мониторингом сна и пульса', 'Гаджеты', 'https://example.com/band.jpg', 4.4, 30, '{"screen": "1.1", "waterproof": "5ATM"}'),
('Кроссовки Nike Air Max', 8990, 'Спортивные кроссовки с технологией Air', 'Одежда', 'https://example.com/shoes.jpg', 4.5, 12, '{"size_range": "38-46", "color": "black/white"}');

INSERT INTO users (email, password_hash, name) VALUES
('test@example.com', '$2b$10$X8zZ2J9J9Z9Z9Z9Z9Z9Z9O', 'Иван Иванов');